# database init, supports mysql too
database=mysql
spring.datasource.url=${MYSQL_URL:jdbc:mysql://localhost/retailer}
spring.datasource.username=${MYSQL_USER:retailer}
spring.datasource.password=${MYSQL_PASS:retailer}
# SQL is written to be idempotent so this is safe
spring.sql.init.mode=always
